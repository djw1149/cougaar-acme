####################################################
# vacate_plugin_insertion.rule
# Rule to insert the VacatePlugin into 
# Robustness Management nodes and agents

society.each_agent(true) do |agent|
  if agent.name =~ /.*RobustnessManager/
    agent.add_component do |c|
      c.classname = "org.cougaar.tools.robustness.ma.plugins.VacatePlugin"
    end
  end
end
